import React, { useContext } from 'react';
import { AuthContext } from '../../Provider/AuthProvider';

const BlogPage = () => {

    

    return (
        <div>
            <h2>this is blogPage</h2>
        </div>
    );
};

export default BlogPage;